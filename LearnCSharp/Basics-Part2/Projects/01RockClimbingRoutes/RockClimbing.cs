using System;
using System.Collections.Generic;
using System.Linq;

namespace LearnCSharp.Basics_Part2.Projects.RockClimbingRoutes
{
    /// <summary>
    /// Create a list of rock climbing users and routes
    /// Seed the Users and Routes with some dummy data, hardcode it in.
    /// A User should have a FirstName and Score, create a class for this
    /// A Route should have Id, Grade, Points, and a Name, create a class for this
    /// Ask the name of the person, add them to the users as a new user
    /// Present the seeded routes as choices, ask which one they did. Add the chosen one to their score
    /// Continue looping to ask what routes they have done until they say they are done
    /// Print all users and their scores at the end
    /// </summary>
    public class RockClimbing
    {
        
    }
}
